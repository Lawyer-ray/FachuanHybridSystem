# Dev 更新记录

用于记录本地 `dev` 分支准备推送的阶段性变更，正式版本发布记录仍以 `CHANGELOG.md` 为准。

## 2026-05-13

### 后台案件

- 新增案件详情“重要时间”功能，支持手动添加、日志提醒同步、状态展示、删除/移除。
- 重要时间复用 `Reminder` 数据，不新增独立案件时间表。
- 增加重要时间相关测试覆盖，包含手动重要时间、日志同步、未勾选不展示、逾期状态等场景。

### 合同文件夹

- 修复合同绑定文件夹时设备号整数溢出问题。
- 修复合同文件夹绑定子目录越界提示乱码。
