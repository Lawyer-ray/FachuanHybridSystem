# v26.54.16

## feat: 新增 contract-numbering skill，实现合同文档自动编号转换

### 背景

合同文档中的手动编号（一、1.（1）①）在修改条款时需要手动重新编号，效率低下且容易出错。需要一个自动化的解决方案将手动编号转换为 Word 自动编号。

### 改动内容

**新增 skills 目录结构**

创建 `skills/` 目录，用于存放可复用的技能模块。每个 skill 都有：
- `README.md`：说明文档
- `CHANGELOG.md`：变更日志
- `{skill_name}.py`：实现代码

**新增 contract-numbering skill**

实现合同文档自动编号转换功能：

- 支持 一、1.（1）① 四级编号格式
- 智能识别文档结构
  - 自动检测一级标题（一、二、三...）
  - 自动检测二级标题（（一）（二）...）
  - 自动检测三级编号（1. 2. 3...）
- 智能推断逻辑
  - 无编号段落自动推断为相应级别
  - 继承上一段落的编号级别
  - 检测"（一）"子标题后自动调整级别
- 排除规则
  - 签字盖章部分自动跳过
  - 以下无正文、签约页等关键词触发跳过
- 输出功能
  - 生成带自动编号的新文档
  - 显示转换映射供用户确认
  - 验证关键段落的编号设置

### 使用方式

```bash
# 命令行调用
backend/.venv/bin/python3 skills/contract-numbering/contract_numbering.py /path/to/document.docx

# 在 Claude 中调用
/contract-numbering /path/to/document.docx
```

### 涉及文件

| 文件 | 变更 |
|------|------|
| `skills/README.md` | 新增 skills 目录说明（+100 行） |
| `skills/contract-numbering/README.md` | 新增 skill 说明文档（+80 行） |
| `skills/contract-numbering/CHANGELOG.md` | 新增变更日志（+40 行） |
| `skills/contract-numbering/contract_numbering.py` | 新增实现代码（+375 行） |
