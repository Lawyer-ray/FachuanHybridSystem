# v26.53.12 — 全面异步化升级（P0/P1 修复）

**日期**: 2026-06-28
**分支**: `perf/async-upgrade-batch-1`
**变更统计**: 38 files changed, +1556, -160

---

## 📋 背景

通过 12 个 opus agent 并行审查 1425 个 Python 文件，发现 451 个可异步化问题（31 P0 / 141 P1 / 279 P2）。第二轮交叉复查后确认 P0 根因并修复。

---

## 🔴 P0 修复（29 个 → 全部解决）

### contracts — 权限检查阻塞事件循环（根因修复）

| 文件 | 修复内容 |
|------|---------|
| `contracts/services/contract/domain/access_policy.py` | 新增 `ahas_access()` / `aensure_access()` 使用 `afilter().aexists()` 替代同步 ORM |
| `contracts/api/contract_api.py` | 5 个 async view 改用 `await aensure_access()` |
| `contracts/api/contractpayment_api.py` | 4 个 view 将 `extract_request_context` 包装 `sync_to_async` |
| `contracts/api/folder_binding_api.py` | 3 个 view 包装 `_require_contract_access` |
| `contracts/api/folder_scan_api.py` | 5 个 view 包装 `_require_contract_access` |
| `contracts/api/supplementary_agreement_api.py` | 5 个 view 改用 `_async_ensure_contract_access()` |

**影响**: 修复了 contracts 模块 23 个 async API 端点直接调用同步 ORM 导致事件循环阻塞的问题。

### plugins — CourtToken 异步获取

| 文件 | 修复内容 |
|------|---------|
| `plugins/message_hub/services/court/court_fetcher.py` | 新增 `_aacquire_token()` / `_ainvalidate_token()` / `_amark_success()` / `_amark_failed()` 使用 async ORM |

### litigation_ai — WebSocket 消费者和 Agent 中间件

| 文件 | 修复内容 |
|------|---------|
| `litigation_ai/agent/interfaces.py` | IMemoryMiddleware ABC 新增 `abefore_agent()` / `aafter_agent()` |
| `litigation_ai/agent/middleware.py` | LitigationMemoryMiddleware 实现 async 版本 |

### doc_convert — 动态 import 阻塞

| 文件 | 修复内容 |
|------|---------|
| `doc_convert/api/doc_convert_api.py` | 将 `_get_doc_convert_service()` 和 `file.read()` 移入 `sync_to_async` 内部 |

---

## 🟡 P1 修复（部分完成）

### core — 邮件/LLM/会话服务

| 文件 | 新增方法 |
|------|---------|
| `core/services/email_service.py` | `asend_password_reset_email()`, `asend_password_changed_notification()` |
| `core/services/pdf_utils.py` | `aread_source_bytes()` 使用 `asyncio.to_thread()` |
| `core/services/conversation_service.py` | `aclear_history()` |
| `core/services/material_classification_service.py` | `_acomplete()` 使用 async LLM |

### cases — API 视图全量 async 化

| 文件 | 修复内容 |
|------|---------|
| `cases/api/caseparty_api.py` | 5 个 view 从 `sync def` 改为 `async def` + `sync_to_async` |
| `cases/api/caseassignment_api.py` | 5 个 view 改为 async |
| `cases/api/caseaccess_api.py` | 5 个 view 改为 async |
| `cases/api/folder_scan_api.py` | 4 个 view 改为 async |
| `cases/services/chat/provider_facade.py` | 新增 `_call_with_timeout_async()` 使用 `asyncio.wait_for()` |

### documents — 服务层 async 包装

| 文件 | 新增方法 |
|------|---------|
| `documents/signals.py` | `log_delete` 改为 `async def` 信号处理器 |
| `documents/services/template/template_matching_service.py` | 4 个 `find_matching_*_async()` 方法 |
| `documents/services/external_template/analysis_service.py` | `analyze_template_async()`, `upload_template_async()` |
| `documents/services/external_template/filling_service.py` | `fill_template_async()`, `batch_fill_async()` |
| `documents/services/extractors/judgment_pdf_extractor.py` | `extract_async()` |
| `documents/services/evidence/evidence_export_service.py` | 3 个 `export_*_async()` 方法 |

### automation — 通知并行化和生命周期优化

| 文件 | 修复内容 |
|------|---------|
| `automation/services/chat/base.py` | ChatProvider ABC 新增 5 个 async 方法（`acreate_chat`, `asend_message`, `asend_file`, `aget_chat_info`, `ais_available`） |
| `automation/services/sms/sms_notification_service.py` | 新增 `asend_case_chat_notification()` 使用 `asyncio.gather()` 并行通知飞书/钉钉/企微/Telegram |
| `automation/models/scraper.py` | 生命周期钩子改为 `submit_task()` 异步提交，不再阻塞 save 事务 |
| `automation/workers/court_sms_tasks.py` | 新增 Django-Q task 入口 |

### litigation_ai — 证据和会话服务

| 文件 | 新增方法 |
|------|---------|
| `litigation_ai/services/evidence/evidence_rag_service.py` | `aensure_ingested()`, `aretrieve()` |
| `litigation_ai/services/evidence/evidence_text_extraction_service.py` | `aextract_chunks()` 使用 `asyncio.to_thread()` |
| `litigation_ai/services/session/session_lifecycle_service.py` | `acreate_session()` |
| `litigation_ai/services/session/session_message_service.py` | `aadd_message()` |
| `litigation_ai/services/session/context_service.py` | `abuild_case_info()` |

### evidence_sorting — 原生 async API

| 文件 | 修复内容 |
|------|---------|
| `evidence_sorting/api/evidence_sorting_api.py` | `classify_images` 和 `parse_statement` 改用原生 async 版本 |

### plugins — 文件操作异步化

| 文件 | 修复内容 |
|------|---------|
| `plugins/court_automation/filing/helpers.py` | `_abuild_materials_map` 文件存在检查改为 `asyncio.to_thread()` |
| `plugins/court_automation/guarantee/helpers.py` | `_collect()` 文件存在检查改为 `asyncio.to_thread()` |

---

## 🧪 测试

| 项目 | 结果 |
|------|------|
| 新增 async e2e 测试 | 44/44 通过 |
| 已有单元测试（受影响模块） | 8252/8253 通过（1 个为已有问题） |
| 集成测试 | 全部通过（修复 evidence_sorting AsyncMock） |
| Django check | ✅ 无问题 |

新增测试文件:
- `tests/ci/unit/test_async_upgrade_e2e.py` — 44 个测试覆盖所有新增 async 方法
- `tests/ci/integration/test_evidence_sorting_api.py` — 修复 AsyncMock 兼容

---

## 📊 第二轮复查结论

通过交叉复查确认：
- **20 个误报**：pdf_splitting / story_viz / workbench / workflow / testing 的 findings 为 task worker 同步上下文，无需修改
- **1 个降级**：plugins/weike_api_private P1→P2（已有 `run_in_executor` 包装）
- **0 个新发现**：全局扫描 2267 个文件未发现遗漏

---

## ⚠️ 未修改项（P2 或已有正确包装）

以下模块的同步代码经审查确认在正确上下文中运行，无需修改：
- Django-Q task worker 中的同步 ORM（automation, pdf_splitting 等）
- 已有 `sync_to_async` 包装的 API 端点
- Django signal handler（Django 6 原生支持 async signal）
- Admin-only 低频端点
- `run_in_executor` 包装的 CPU 密集操作
