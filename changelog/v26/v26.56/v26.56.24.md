# v26.56.24

> 分支：`feat/materials-batch-delete`

## 后端

### 新功能

#### feat(materials): 材料页支持批量删除未绑定附件

- 新增 `DELETE /api/v1/cases/{case_id}/materials/attachments` 接口，仅允许删除未绑定到材料（`CaseMaterial`）的日志附件，带权限校验
- 补充 `case_material_service` 批量删除服务与 Ninja schema，接口与服务的单元测试、集成测试同步覆盖

### 修复

#### fix(cases): bind-candidates 补空文件防护

- 材料绑定候选查询对空文件附件补空值防护，修复案件详情页与绑定候选接口的 500 错误

#### fix(contracts): 归档材料读取补文件路径缺失防护

- 归档文件下载/预览在 `file_path` 为空时不再抛 `Path(None)` TypeError，改为跳过并返回友好提示
- `download_handler`（`_read_material_file` / `_merge_materials_to_pdf`）、`pdf_utils`（缩放 / 合并 / 案卷编译）、`archive_api` 预览端点、admin 预览视图统一补齐空路径防护
- 存量被清空的 `file_path` 数据已通过一次性脚本从 `materials_material` 回填（归档材料 1668 条全部恢复，文件均在磁盘）

## 前端

### 新功能

#### feat(materials): 材料页批量删除交互

- 材料管理表格新增行复选框与「删除选中」按钮，删除前弹确认框，完成后自动刷新列表

### 修复

#### fix(cases): 材料页清空按钮

- 「清空」按钮改为清除全部筛选条件（此前仅重置部分条件，列表看似无响应）
