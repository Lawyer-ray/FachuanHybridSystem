# v26.51.11 - 2026-06-08

## 后端

### 测试
- **单元测试全覆盖**: 新增 311 个测试文件，覆盖全部业务模块（automation、cases、contracts、core、documents、client、evidence、legal_research、litigation_ai 等）
- **测试数量**: 从约 3,700 个测试增长到约 7,200 个测试
- **覆盖率提升**: 后端代码覆盖率从 47% 提升到 48%（122K 行代码）
- **测试文件规范化**: 所有测试文件统一放在 `tests/ci/unit/` 目录下，按业务模块组织

### 优化
- **pytest 配置优化**: 启用 pytest-xdist 并行测试（`-n auto --dist loadfile`），预期提速 4-8 倍
- **pytest 配置优化**: 将 `-v` 改为 `-q`，减少终端输出
- **pytest 配置优化**: 添加 `--timeout=30`，防止测试挂死
- **CI 优化**: 合并 CI 中的两次 coverage 运行为一次，减少 CI 时间
- **删除冗余测试**: 删除 `test_regression_suite.py`，避免全量 import 371 个模块
- **修复未 mock 的 sleep**: 将 `time.sleep(10)` 替换为 `time.sleep(1)`

### 修复
- **format_html 修复**: 修复 `apps/automation/admin/sms/court_sms_admin_base.py` 中的 format_html 调用
- **performance_monitor_service_adapter**: 优化性能监控服务适配器
- **evidence_admin_service**: 修复证据管理服务

## 前端

### 测试
- **前端测试从零到有**: 新增 347 个测试文件，2,740 个测试用例
- **覆盖率大幅提升**: 前端代码覆盖率从 0.48% 提升到 61.17%
- **全模块覆盖**: 覆盖全部 16 个 feature 模块（auth、cases、clients、contacts、contracts、content-ops、dashboard、inbox、message-sources、organization、reminders、settings、templates、tools、workbench、automation）
- **测试层次完整**: 覆盖 lib（工具函数）、stores（状态管理）、hooks（自定义 hooks）、api（API 调用）、components（组件）、pages（页面）、layouts（布局）、routes（路由）

### 新增测试覆盖
- **lib 层**: 10 个测试文件（api、date、format、token、clipboard、download 等）
- **stores 层**: 2 个测试文件（auth、ui）
- **hooks 层**: 3 个测试文件（use-debounce、use-paginated-list、use-speech-recognition）
- **features 层**: 约 300 个测试文件，覆盖所有 feature 模块的 api、hooks、components
- **pages 层**: 20 个测试文件，覆盖所有页面
- **layouts 层**: 6 个测试文件（AuthLayout、AdminLayout、Breadcrumb、Navbar、Sidebar、menu-config）
- **routes 层**: 4 个测试文件（paths、guards、route-error、index）

### 配置
- **vitest 配置**: 添加 coverage 配置，排除 `components/ui/`（shadcn/ui 基础组件）
- **test-setup**: 添加 `ResizeObserver` polyfill，兼容 Radix UI
- **依赖**: 安装 `@testing-library/user-event`、`vitest`、`@vitest/coverage-v8`

## 基础设施

### CI/CD
- **backend-ci.yml**: 合并两次 coverage 运行为一次，减少 CI 时间
- **backend-ci.yml**: 添加 `-n auto --dist loadfile` 并行测试
- **backend-ci.yml**: 将 `-v` 改为 `-q`

### 依赖更新
- **后端**: 安装 pytest-xdist、pytest-timeout
- **前端**: 安装 @testing-library/user-event、vitest、@vitest/coverage-v8

## 统计

| 指标 | 数值 |
|------|------|
| 新增文件数 | 663 |
| 新增代码行数 | 90,513 |
| 后端测试文件数 | 311 |
| 前端测试文件数 | 347 |
| 后端测试用例数 | ~7,200 |
| 前端测试用例数 | 2,740 |
| 后端覆盖率 | 48% |
| 前端覆盖率 | 61.17% |
