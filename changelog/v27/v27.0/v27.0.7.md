# v27.0.7

`apps/core` 死代码清理与架构整改：删除约 2600 行经全仓验证零生产引用的死代码、收敛冗余间接层、修复 4 个存量 import 失败与 9 个 mypy 错误，并新增 203 个端到端测试用例。

## 前端

本次无前端改动。

## 后端

### 移除（死代码，均经全仓验证零生产引用）

- core：删除 `config/steering/` 整个目录（1971 行）——Kiro IDE steering-files 机制被误移植进 Django config 包，仓库不存在其服务的 `.kiro/steering` 规范目录，0 生产引用、0 配置消费，13 个测试文件纯为覆盖率存在
- core：删除 `config/steering_config_template.yaml`、`config.yaml` / `config.example.yaml` 的 `steering:` 配置段、`config/schema/_registry_features.py` 的 4 个 `steering.*` ConfigField 注册
- core：删除 `infrastructure/event_bus.py` + `events.py`——自称 compatibility event bus，生产代码零使用
- core：删除 `infrastructure/tracing.py`——`get_current_trace_ids()` 与 `infrastructure/request_context.get_trace_ids()` 功能完全重复；`middleware/request_id.py` 改调后者
- core：删除 `interfaces/` 下 6 个死 shim（`automation_protocols` / `case_protocols` / `contract_protocols` / `document_protocols` / `organization_protocols` / `dtos`，共 172 行纯 re-export，零直接消费者）
- core：删除 `llm/costs.py`（成本统计，仅测试引用）、`dependencies/oa_filing.py`（5 个 build 函数 0 引用且未被转发）、`utils/chinese_format.py`（整文件 0 引用）
- core：删除 `cloud_storage/` 空壳目录（已拆分为独立 app，仅剩 `__pycache__`）
- core：删除 4 个零引用管理命令：`collect_court_templates` / `test_court_template_collection` / `ensure_cloakbrowser` / `start_resource_monitor`
- core：删除 `documents/models/evidence.py`（380 行残留副本，与 `apps/evidence/models/evidence.py` 重复注册 `EvidenceList`）

### 优化（架构整改）

- core：收敛 `protocols/common_protocols.py` 中间跳转层，`protocols/__init__.py` 改为直接 `from .common import`
- core：`interfaces/service_locator.py` 只保留 `ServiceLocator` 转发（`EventBus` / `Events` 随源文件一并移除），消除 shim 的 shim
- core：`api/__init__.py` 不再直连 `SystemConfigRepository`（违反四层架构），5 个 system-config 端点全部改走 `SystemConfigService`；`SystemConfigService` 新增 `get_all_active_configs()` / `get_config_by_key()` 门面方法
- core：`SystemConfigService.aget_value` 从直连 `SystemConfig.objects` 改为走 `SystemConfigRepository.aget_active_by_key`（repo 新增该方法），消除同一 service 内两种数据访问风格并存
- core：`LLMProviderService._load_from_db` 与 `ParseProviderService._load_from_db` 由 `@staticmethod` 改为 `@classmethod`，满足「Service 层禁止 `@staticmethod`」规范
- core：清理无效导出——`utils/__init__.py` 的 `__all__` 原列 12 个全仓无定义无调用的名字；`constants.py` 8/9 个常量 0 引用，仅保留 `LARGE_FILE_MAX_SIZE`
- core：`llm/model_list_service.py` 一处 `except Exception: pass` 补 `logger.warning`，Ollama 模型 context_window 获取失败不再静默返回空
- core：`apps.py` 清理已迁移的 cloud_storage 陈旧注释
- core：修正 `README.md` 失效的目录结构（原描述 11 个顶层文件全部不存在，core 早已重组为 27 个子目录）与 6 处死链引用

### 修复（存量 bug，此前模块完全不可用）

- automation：`automation_service_adapter.py` 修正 import 不存在的 `ValidationError`（`apps.core.exceptions` 从未导出该名，只有 `ValidationException`）——该模块因此**从未成功 import 过**，`except ValidationException` 分支实为死代码
- automation：新建 `litigation/dtos.py`，定义 `CourtPleadingSignals` pydantic 模型（5 字段，与 `core/dto/litigation.py` 的 DTO 对齐），修复 `litigation/schemas.py` 与 `court_pleading_signals_service.py` 的 `from .dtos import` 指向不存在文件
- core：`tasking/qcluster_spawn.py` 给函数挂属性改用 `setattr`，消除 mypy `attr-defined` 错误
- core：`utils/validators.py`、`management/commands/init_system_config.py` 删除多余的 `type: ignore`
- CI：修正 `.github/workflows/backend-ci.yml` 与 `Makefile` 门禁配置失效——curated gate 硬编码引用已删的 `protocols/common_protocols.py`、extra gate 引用不存在的 `apps/automation/services/token/cache_manager.py`（实际在 `plugins/` 下）

### 测试

- core：新增 5 个端到端测试文件共 203 用例，全部走真实 import / 真实 DB / 真实 HTTP：系统配置 CRUD 全链路、LLM/Provider 链路（含 `staticmethod` → `classmethod` 行为等价与 TTL 缓存）、automation 修复点首次真实可用、RequestIdMiddleware 热路径与配置体系、架构守卫（ServiceLocator 双入口身份一致 / EventBus 彻底消失 / 全仓 import 冒烟 / API 路由注册）
- core：删除 13 个 steering 相关测试与 llm costs 测试，精简 3 个混合测试文件，修正 mock 已删符号的用例
- core：更新结构守卫 `test_core_no_business_deps.py` 冻结基线（移除已删的 `dependencies/oa_filing.py` 条目，基线只减不增）

### 验证

- 全仓 `tests/ci/unit/` + `tests/ci/structure/`：**28315 passed, 0 failed, 0 error**
- `tests/ci/integration/` 348 passed、`tests/ci/property/` 378 passed
- 全仓 import 冒烟 1878 模块全部成功；CI 三个 mypy 门禁（changed-files / curated / extra）全部通过；ruff check + format 通过
- mypy 错误数与 main 基线完全一致（9 个，全在 migrations，非本次引入）
