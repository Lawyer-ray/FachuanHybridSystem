# v26.56.23

> 分支：`refactor/message-hub-return-to-app`

## 后端

### 重构

#### refactor(message_hub): 收件箱功能从插件迁回 Django app

- 收件箱通用能力（Admin / API / Schemas / Services / IMAP / 任务调度 / 静态资源 / 模板）整体迁回 `apps.message_hub`，不再依赖插件
- `/api/v1/inbox` 路由改由 `apps.message_hub.api` 直接注册；`settings.py` 启用 `MessageHubConfig`，移除插件模板与静态目录的条件加载
- `admin_customization.py` 移除插件懒加载 admin 注册与 `has_message_hub_plugin` 上下文变量，收件箱入口改为「其他工具」聚合页静态条目；`base_site.html` 顶部「收件箱 / 消息来源」快捷链接常驻显示
- 全局 `submit_line.html` admin 模板覆盖迁移至 `apps/organization/templates/admin/`
- 「一张网收件箱 / 庭审日程」适配器（`court_fetcher.py` / `court_schedule_fetcher.py`）保留在 `plugins/message_hub/services/court/`，`apps.message_hub.services.get_fetcher` 工厂按来源类型懒加载
- `backend/plugins` 子模块同步更新：插件定位收敛为「一张网扩展 v2.0.0」，仅保留法院系统（zxfw.court.gov.cn）适配器
- 测试引用路径同步更新，lint 与 message_hub 单元测试全部通过

### 优化

#### chore: `.iwe/` 本地会话目录加入 gitignore

- `.iwe/` 目录（本地会话数据库 / 工作区文件）整体加入 `.gitignore`，不再入库
