# v26.51.10 - 2026-06-08

## 后端

### 新增
- **单元测试全面覆盖（311 个文件）**：覆盖全部 31 个业务模块的单元测试
  - automation（sms、token、document_delivery、filing_helpers、guarantee、gsxt、insurance 等）
  - batch_printing、cases、client、contacts、content_ops、contract_review、contracts
  - core、doc_converter、documents、enterprise_data、express_query、fee_notice
  - image_rotation、invoice_recognition、legal_research、litigation_ai
  - message_hub、oa_filing、organization、pdf_splitting、preservation_date
  - reminders、sales_dispute、story_viz、wechat_mp、workbench

### 优化
- **pytest.ini 并行测试 + 超时**：新增 `-n auto --dist loadfile`（pytest-xdist 并行）和 `--timeout=30`（单测试超时 30s），输出模式从 `-v` 改为 `-q`
- **performance_monitor_service_adapter.py**：`success=True/False` 布尔字段查询改为 `TokenAcquisitionStatus.SUCCESS` 枚举，适配模型字段变更

## 前端

### 新增
- **单元测试全面覆盖（340 个文件）**：覆盖 16 个特性模块 + 5 个基础模块
  - 特性模块：auth、automation、cases、clients、contacts、content-ops、contracts、dashboard、inbox、message-sources、organization、reminders、settings、templates、tools、workbench
  - 基础模块：components/shared、contexts、lib、providers、routes、stores
- **@testing-library/user-event**：新增 `@testing-library/user-event ^14.6.1` 开发依赖，支持更真实的用户交互模拟

### 优化
- **vite.config.ts 覆盖率配置**：新增 v8 coverage provider，include `src/**/*.{ts,tsx}`，排除 UI 组件库、测试文件、类型声明和入口文件
- **test-setup.ts ResizeObserver polyfill**：为 jsdom 环境补充 `ResizeObserver` polyfill，解决 Radix UI 组件在测试中的报错

## CI/CD

### 优化
- **backend-ci 合并覆盖率 jobs**：将 `Coverage (unit)` 和 `Coverage (apps global baseline)` 两个独立 step 合并为一个，复用同一 pytest 运行，新增 `-n auto --dist loadfile` 并行执行
