# Django Admin 稳定性测试 - 快速开始指南

## 5 分钟快速开始

### 步骤 1: 确认环境（1 分钟）

```bash
# 1. 确认 Django 服务器正在运行
# 在一个终端窗口中：
cd backend
source venv311/bin/activate
python manage.py runserver

# 2. 确认可以访问 Admin
# 在浏览器中打开: http://localhost:8000/admin/
# 使用账号登录: 法穿 / 1234qwer
```

### 步骤 2: 准备测试数据（2 分钟）

```bash
# 在另一个终端窗口中：
cd backend
source venv311/bin/activate

# 创建测试数据
python manage.py shell < tests/admin/scripts/create_test_data.py
```

**预期输出**:
```
==============================================================
创建测试数据
==============================================================

当前数据统计:
  律所: 0
  律师: 0
  客户: 0
  合同: 0
  案件: 0

开始创建测试数据...

1. 创建律所...
   ✅ 创建律所: 测试律所 (ID: 1)

2. 创建律师...
   ✅ 创建律师: 测试律师1 (ID: 1)
   ✅ 创建律师: 测试律师2 (ID: 2)
   ✅ 创建律师: 测试律师3 (ID: 3)

3. 创建客户...
   ✅ 创建客户: 测试客户1 (ID: 1)
   ...

==============================================================
测试数据创建完成！
==============================================================
```

### 步骤 3: 运行冒烟测试（2 分钟）

```bash
cd backend/tests/admin
python run_admin_tests.py --suite smoke
```

**预期输出**:
```
======================================================================
🚀 Django Admin 稳定性测试
======================================================================
开始时间: 2024-12-01 10:00:00
======================================================================

======================================================================
📋 冒烟测试 - 页面访问
======================================================================

发现 3 个测试用例

[1/3] All List Pages Accessible... ✅ PASSED
[2/3] Admin Home Page... ✅ PASSED
[3/3] Logout... ✅ PASSED

======================================================================
📊 测试总结
======================================================================

总计测试: 3
  ✅ 通过: 3
  ❌ 失败: 0
  ⏭️  跳过: 0

成功率: 100.0%

======================================================================
结束时间: 2024-12-01 10:02:30
======================================================================
```

## 完成！🎉

如果冒烟测试全部通过，说明你的 Admin 基本功能正常。

## 下一步

### 运行完整测试（10 小时）

```bash
python run_admin_tests.py --all
```

### 运行特定测试套件

```bash
# 只运行 CRUD 测试
python run_admin_tests.py --suite crud

# 只运行内联表单测试
python run_admin_tests.py --suite inline

# 只运行表单验证测试
python run_admin_tests.py --suite validation

# 只运行 Admin Action 测试
python run_admin_tests.py --suite action

# 只运行性能测试
python run_admin_tests.py --suite performance
```

## 使用 Playwright MCP（推荐）

如果你已经配置了 Playwright MCP，可以在 Kiro 中直接运行测试：

1. 打开 Kiro
2. 在聊天中输入：`运行 Admin 冒烟测试`
3. Kiro 会使用 Playwright MCP 执行测试并显示结果

## 常见问题

### Q: 测试失败：找不到元素

**A**: 可能是页面结构变化或加载时间过长

**解决方法**:
1. 检查选择器是否正确
2. 增加等待时间
3. 截图调试

### Q: 测试失败：超时

**A**: 页面加载时间过长

**解决方法**:
1. 检查 Django 服务器是否正常运行
2. 检查网络连接
3. 增加超时时间

### Q: 测试跳过：没有测试数据

**A**: 数据库中没有必要的测试数据

**解决方法**:
```bash
python manage.py shell < tests/admin/scripts/create_test_data.py
```

### Q: 浏览器没有启动

**A**: Playwright 浏览器未安装

**解决方法**:
```bash
playwright install chromium
```

## 查看测试报告

测试完成后，会在控制台输出详细的测试报告。

如果需要生成 HTML 报告，可以使用：

```bash
python run_admin_tests.py --all --report html
```

报告会保存在 `backend/tests/admin/reports/` 目录中。

## 获取帮助

```bash
# 查看帮助信息
python run_admin_tests.py --help

# 查看详细文档
cat README.md
cat TESTING_PLAN.md
cat EXECUTION_TASKS.md
```

## 联系方式

如有问题，请联系：
- **测试负责人**: Kiro AI
- **项目负责人**: 法穿
