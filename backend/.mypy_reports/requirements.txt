# Django Core
django==6.0.1
django-ninja==1.5.1
django-ninja-jwt>=5.4.3
django-ninja-extra>=0.30.8  # django-ninja-jwt 的依赖

# WebSocket Support
channels>=4.3.2
channels-redis>=4.2.0
daphne>=4.2.1  # ASGI server for Channels

# Database & Cache
# 使用 Django 内置 LocMemCache；生产可选 Redis cache（DJANGO_CACHE_REDIS_URL）
redis>=5.0.0
psycopg[binary]==3.3.2

# Background Tasks
django-q2>=1.9.0

# Data Processing
pydantic>=2.12.5
pandas>=2.3.3
openpyxl>=3.1.5

# HTTP Client
httpx>=0.28.1  # Async HTTP client for insurance API

# WSGI Server
gunicorn>=23.0.0  # Production WSGI server

# Document Processing
playwright>=1.57.0
docxtpl>=0.20.2
reportlab>=4.0.0
opencv-python>=4.8.0  # ID card image processing
pikepdf>=9.0.0
pymupdf>=1.26.7  # PDF processing (fitz module)
pdfplumber>=0.11.9  # PDF text extraction
python-docx>=1.2.0  # Word document processing
rapidocr>=3.5.0  # OCR for document text extraction (PP-OCRv5 support)

# OCR for Captcha (双引擎策略)
ddddocr>=1.5.6  # 快速验证码识别引擎
# rapidocr 已在上方 Document Processing 中引入，作为兜底引擎
langchain-core>=1.2.7
langchain-openai>=1.1.7

# System Monitoring
psutil>=7.2.1  # System and process monitoring for health checks
sentry-sdk>=2.0.0

# Utilities
python-dotenv>=1.2.1
python-dateutil>=2.9.0.post0  # Date manipulation for contract renewal
watchdog>=6.0.0  # File system monitoring for config hot reload
pyyaml>=6.0.3  # YAML configuration support

# CORS & Security
django-cors-headers>=4.9.0
cryptography>=46.0.3  # 爬虫配置加密

# Admin
django-nested-admin>=4.1.6

# Code Quality (optional)
# black>=24.0.0
# isort>=5.13.0
# flake8>=7.0.0
# mypy>=1.8.0
